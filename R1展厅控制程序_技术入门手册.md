# R1 展厅控制程序 - 技术入门手册

> 本文档面向零基础读者，用最直白的语言解释项目中用到的所有技术。
> 如果你已经有一定基础，请参考 `R1展厅控制程序_技术学习报告.md`。

---

## 目录

1. [整体架构：程序是怎么跑起来的？](#1-整体架构)
2. [C++：我们的主要编程语言](#2-c是什么)
3. [Unitree SDK2：控制机器人的工具包](#3-unitree-sdk2是什么)
4. [DDS：机器人各部分之间的"对讲机"](#4-dds通信协议)
5. [systemd：让程序开机自动运行](#5-systemd服务管理)
6. [Bash 脚本：一键完成所有操作的"批处理"](#6-bash脚本)
7. [各部分是怎么连接起来的？](#7-连接关系图)
8. [新手常见问题](#8-新手常见问题)

---

## 1. 整体架构

### 一句话概括

我们的程序 = **一段 C++ 代码**，运行在 R1 机器人的电脑里，通过 **Unitree SDK2** 控制机器人的**喇叭**、**LED 灯**和**机械臂**，同时通过 **DDS 通信**接收遥控器的指令。

### 程序运行流程图

```
┌─────────────────────────────────────────────────────┐
│                  R1 机器人内部                      │
│  ┌─────────────────────────────────────────────┐  │
│  │    我们的程序 (r1_show_control)              │  │
│  │    ↓ 使用 Unitree SDK2                     │  │
│  │  ├─ 控制喇叭 → 播放欢迎语音                │  │
│  │  ├─ 控制 LED → 改变灯光颜色                │  │
│  │  └─ 控制机械臂 → 挥手/比心等动作          │  │
│  │                                              │  │
│  │    ↑ 通过 DDS 接收                          │  │
│  │    遥控器的按键指令                          │  │
│  └─────────────────────────────────────────────┘  │
│                                                    │
│  [开机自启动] ← systemd 服务管理                   │
│  [日志输出]   → /var/log/r1_show_control.log      │
└─────────────────────────────────────────────────────┘

         ↑ 无线连接
    ┌─────────┐
    │ 遥控器   │ → 按键 → DDS 消息 → 程序接收
    └─────────┘
```

---

## 2. C++ 是什么？

### 基本概念

**C++** 是一种**编程语言**，就像英语是一种人类语言一样。我们用 C++ 写代码，电脑（机器人）就能理解并执行。

### 为什么选 C++？

| 语言 | 速度 | 难度 | 适用场景 |
|------|------|------|---------|
| Python | 慢 | 简单 | 数据分析、AI |
| **C++** | **极快** | 较难 | **机器人、游戏、操作系统** |
| Java | 中等 | 中等 | 企业应用 |

**机器人需要实时响应**，所以必须用 C++。

### 我们的代码里 C++ 做什么？

```cpp
// 1. 包含头文件（引入工具包）
#include "unitree/robot/Channel.hpp"
#include "unitree/robot/Audio.hpp"

// 2. 创建对象（实例化工具）
unitree::robot::Audio audio;  // 音频播放器
unitree::robot::Led led;      // LED 控制器

// 3. 调用函数（发指令）
audio.Speak("/path/to/voice.mp3", 50);  // 播放语音，50% 音量
led.White();                                  // LED 亮白灯
```

### C++17 是什么？

**C++17** 是 C++ 在 2017 年发布的版本，增加了一些新功能，让我们的代码更简洁。

---

## 3. Unitree SDK2 是什么？

### 基本概念

**SDK = Software Development Kit（软件开发工具包）**

你可以把它理解为一个**工具箱**，里面有很多现成的工具，让你不用从零开始造轮子。

### Unitree SDK2 提供了什么？

| 工具类 | 功能 | 使用示例 |
|--------|------|---------|
| `Audio` | 播放声音 | `audio.Speak(path, volume)` |
| `Led` | 控制 LED 灯 | `led.Red()` / `led.Green()` |
| `ChannelFactory` | 创建 DDS 通信通道 | `create_publisher()` |
| `RobotState` | 查询机器人状态 | `get_battery()` |

### SDK 的文件结构

```
unitree_sdk2-1.0/
├── include/              # 头文件（说明书）
│   └── unitree/
│       └── robot/
│           ├── Audio.h   # 音频播放说明书
│           ├── Led.h     # LED 控制说明书
│           └── Channel.h # DDS 通信说明书
├── lib/
│   └── aarch64/        # 编译好的库文件（工具本身）
│       └── libunitree_sdk2.so
└── thirdparty/          # 第三方工具
    └── lib/aarch64/
        └── libddsc.so   # DDS 通信库
```

### 为什么需要 `#include`？

```cpp
#include "unitree/robot/Audio.hpp"
```

这行代码的意思是：**"把我需要用到的工具说明书引进来"**。

没有它，编译器就不知道 `Audio` 是什么东西。

---

## 4. DDS 通信协议

### 基本概念

**DDS = Data Distribution Service（数据分发服务）**

你可以把它理解为机器人内部的**"对讲机系统"**：

```
传统方式（函数调用）：
  程序 → 直接调用函数 → 机械臂动

DDS 方式（发布/订阅）：
  程序 → 发布消息："机械臂，做动作 #26"
  ↓ 通过 DDS 对讲机广播
  机械臂控制器 → 收到消息 → 执行动作 #26
```

### 为什么要用 DDS？

| 优势 | 说明 |
|------|------|
| **解耦** | 程序不需要知道机械臂怎么工作，只要发消息 |
| **实时** | DDS 延迟极低，适合机器人控制 |
| **可靠** | 消息不丢失，保证送达 |

### DDS 的核心概念

#### **发布者（Publisher）**
- 发送消息的一方
- 比如：我们的程序发布"机械臂动作指令"

#### **订阅者（Subscriber）**
- 接收消息的一方
- 比如：机械臂控制器订阅"动作指令"

#### **话题（Topic）**
- 消息的类型/频道
- 比如："arm_commands" 话题专门用来发送机械臂指令

### 我们的代码里怎么用 DDS？

```cpp
// 1. 创建发布者（拿到一个"对讲机"）
auto arm_pub = sdk.create_publisher<ArmMsg>("arm_commands");

// 2. 准备消息
ArmMsg msg;
msg.arm_action_id = 26;  // 动作编号：高举挥手

// 3. 发送消息（按下对讲机说话按钮）
arm_pub->Write(msg);
```

### DDS 通信示意图

```
┌─────────────────┐         DDS 对讲机系统         ┌─────────────────┐
│   我们的程序     │ ─── 发布消息 ───→           │  机械臂控制器    │
│                 │         "动作#26"             │  → 执行挥手动作  │
└─────────────────┘                              └─────────────────┘

┌─────────────────┐         DDS 对讲机系统         ┌─────────────────┐
│   遥控器        │ ─── 发布消息 ───→           │   我们的程序      │
│                 │         "按键按下"             │  → 播放语音+动作 │
└─────────────────┘                              └─────────────────┘
```

---

## 5. systemd 服务管理

### 基本概念

**systemd** 是 Linux 系统的**"程序管理员"**。

### 没有 systemd 的问题

```
传统方式：
1. 机器人开机
2. 你需要手动 SSH 进去
3. 手动运行程序：./r1_show_control eth10
4. 如果程序崩溃了，你还得重新手动启动
```

### 有了 systemd 之后

```
使用 systemd：
1. 机器人开机
2. systemd 自动启动程序 ✅
3. 如果程序崩溃，systemd 自动重启 ✅
4. 你只需要关心：程序有没有在运行
```

### systemd 服务文件

我们创建了一个服务文件 `/etc/systemd/system/r1-show.service`：

```ini
[Unit]
Description=Unitree R1 Showroom Control
After=network-online.target

[Service]
Type=simple
ExecStart=/usr/local/bin/r1_show_control eth10
Restart=always    # 崩溃后总是重启
RestartSec=5      # 等待 5 秒再重启

[Install]
WantedBy=multi-user.target  # 开机自启动
```

### 常用 systemd 命令

```bash
# 启动服务
sudo systemctl start r1-show.service

# 停止服务
sudo systemctl stop r1-show.service

# 查看状态（看程序是否运行）
sudo systemctl status r1-show.service

# 启用开机自启动
sudo systemctl enable r1-show.service
```

---

## 6. Bash 脚本

### 基本概念

**Bash** 是 Linux 的**命令行解释器**，你可以把它理解为 Windows 的 `cmd` 或 PowerShell。

**Bash 脚本** = 把多条命令写在一个文件里，一键执行。

### 我们的部署脚本做了什么？

**文件名：** `deploy_r1.sh`

```bash
#!/bin/bash
# 这一步：检查文件是否存在
if [ ! -f "r1_show_control.cpp" ]; then
    echo "错误：找不到源代码！"
    exit 1
fi

# 第二步：编译（把 C++ 代码翻译成机器人能懂的机器码）
g++ -std=c++17 r1_show_control.cpp -o r1_show_control

# 第三步：部署（把编译好的程序复制到系统目录）
sudo cp r1_show_control /usr/local/bin/

# 第四步：重启服务（让新的程序生效）
sudo systemctl restart r1-show.service
```

### 为什么需要脚本？

| 手动操作 | 脚本操作 |
|---------|---------|
| 需要记住 7 个步骤 | 只需运行 `./deploy_r1.sh` |
| 容易漏掉某一步 | 脚本不会漏 |
| 每次更新都要重复 7 步 | 一键完成 |

---

## 7. 各部分是怎么连接起来的？

### 完整连接关系图

```
┌─────────────────────────────────────────────────────────────┐
│                       硬件层                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐            │
│  │  喇叭     │  │  LED灯   │  │ 机械臂   │            │
│  └──────────┘  └──────────┘  └──────────┘            │
│       ↑               ↑               ↑                   │
└───────┼───────────────┼───────────────┼───────────────────┘
        │               │               │
┌───────┼───────────────┼───────────────┼───────────────────┐
│       ↓               ↓               ↓                   │
│  ┌─────────────────────────────────────────────┐        │
│  │     Unitree SDK2（硬件控制接口）             │        │
│  │     Audio类 / Led类 / Channel类            │        │
│  └─────────────────────────────────────────────┘        │
│                         ↓                               │
│  ┌─────────────────────────────────────────────┐        │
│  │         我们的程序 (C++ 代码)               │        │
│  │  1. 初始化 SDK                            │        │
│  │  2. 创建 DDS 通信                        │        │
│  │  3. 等待遥控器指令                        │        │
│  │  4. 收到指令后：播放语音 + 控制机械臂    │        │
│  └─────────────────────────────────────────────┘        │
│                         ↑                               │
└─────────────────────────┼───────────────────────────────┘
                          │
                 ┌────────┴────────┐
                 │    DDS 通信     │
                 │   （对讲机）    │
                 └────────┬────────┘
                          │
                 ┌────────┴────────┐
                 │    遥控器         │
                 │   （用户操作）    │
                 └─────────────────┘
```

### 数据流向

```
用户按下遥控器按键
        ↓
遥控器通过 DDS 发送消息："{buttons: 0x02|0x10}"
        ↓
我们的程序收到 DDS 消息
        ↓
程序判断：这是"← + A"组合键
        ↓
程序调用 Unitree SDK2：
  1. Audio.Speak() → 播放欢迎语音
  2. arm_pub->Write() → 发送机械臂动作指令
        ↓
DDS 把指令转发给机械臂控制器
        ↓
机械臂执行动作（挥手）
        ↓
任务完成！
```

---

## 8. 新手常见问题

### Q1：什么叫"编译"？

**A：** C++ 代码是给人看的，机器人看不懂。编译就是把"人话"翻译成"机器话"的过程。

```
源代码 (r1_show_control.cpp)  ─── g++ 编译 ──→  可执行文件 (r1_show_control)
      ↑                                          ↑
   人能看懂                                  机器人能执行
```

### Q2：为什么有这么多层？

**A：** 每一层都有自己的职责：

```
我们的代码（最上层）
    ↓ 调用
Unitree SDK2（中间层：统一接口）
    ↓ 调用
DDS / 系统调用（底层：实际硬件操作）
    ↓ 控制
硬件（最底层：喇叭、LED、机械臂）
```

这样做的好处：**如果硬件换了，我们只需要改 SDK，不用改我们的代码。**

### Q3：DDS 和 HTTP 有什么区别？

| 特性 | HTTP | DDS |
|------|------|-----|
| **速度** | 慢（需要建立连接） | 极快（直接发送） |
| **适用场景** | 网页、API | 机器人、自动驾驶 |
| **连接方式** | 请求-响应 | 发布-订阅 |
| **实时性** | 差 | 极强 |

### Q4：我不会 Linux，能学会吗？

**A：** 能！你只需要记住 5 个命令：

```bash
ls        # 查看当前目录有什么文件
cd 路径   # 进入某个目录
sudo systemctl status 服务名    # 查看程序是否运行
tail -f 日志文件               # 查看实时日志
./deploy_r1.sh                 # 一键部署
```

### Q5：aarch64 到底是什么意思？

**A：** 简单说就是**机器人的 CPU 类型**。

- 你的电脑：Intel/AMD CPU（x86_64 架构）
- R1 机器人：ARM CPU（aarch64 架构）

**关键：** 电脑上编译的程序，机器人不能直接用。必须在机器人上重新编译。

---

## 附录：技术栈总结表

| 技术 | 类型 | 在项目中的角色 |
|------|------|---------------|
| **C++17** | 编程语言 | 写我们的主程序 |
| **Unitree SDK2** | 开发工具包 | 提供控制机器人的接口 |
| **DDS** | 通信协议 | 遥控器 ↔ 程序 ↔ 机械臂 |
| **systemd** | 服务管理 | 开机自启动 + 崩溃重启 |
| **Bash** | 脚本语言 | 一键编译、部署、启动 |
| **logrotate** | 日志管理 | 自动清理旧日志 |

---

## 下一步学习建议

1. **先跑起来**：把程序部署到机器人上，实际看看效果
2. **改改参数**：试着改一下 LED 颜色、语音音量
3. **加个新场景**：在 `SCENE` 数组里加一段新话术 + 新动作
4. **深入理解**：回头看 `R1展厅控制程序_技术学习报告.md`

---

**文档版本：** 1.0  
**适用读者：** 零基础新手  
**更新日期：** 2026-05-13
