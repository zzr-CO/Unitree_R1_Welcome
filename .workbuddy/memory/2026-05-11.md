# 2026-05-11 工作日志

## 宇树 R1 机器人展厅接待程序开发

### 问题描述
- 用户运行 `r1_show_control` 时报错"未检测到手柄设备"
- 原因：机器人上运行的是旧版本（读取本地手柄 `/dev/input/js*`）
- 代码库中的是新版本（通过 DDS 读取宇树遥控器）

### 解决方案
- 确认使用**宇树官方遥控器**（DDS 版本），不依赖本地手柄
- 重新编译最新的 `r1_show_control.cpp`

### 代码修改（r1_show_control.cpp）
1. **更新 6 段早安问候话术**：
   - 用户指定 5 段 + AI 生成 1 段（"早安！感恩新的一天，愿你工作顺心，收获满满～"）
2. **简化动作**：
   - 删除所有复杂动作（脸部挥手25、手放胸口33、右手举起23、双手比心20）
   - 所有场景统一使用**高举挥手(26)**
3. **保留功能**：
   - ← + A：语音+动作同步
   - ← + B：仅语音播报

### 编译命令
```bash
g++ -std=c++17 r1_show_control.cpp \
    -I./include -I./thirdparty/include/ddscxx \
    -L./lib/aarch64 -L./thirdparty/lib/aarch64 \
    -lunitree_sdk2 -lddscxx -lddsc -lpthread \
    -Wl,-rpath,./lib/aarch64:./thirdparty/lib/aarch64 \
    -o r1_show_control
```

### 讨论：编译命令格式
- 两条编译命令功能完全相同
- 区别仅在于：换行符位置、源文件名、输出文件名
- `-I` 和 `-L` 参数在同一行或分多行写，编译器处理完全一致

---

## 项目文件状态
- `D:\unitree\r1_show_control.cpp`：已修改（统一动作+新话术）
- `D:\unitree\r1_showroom_reception.cpp`：未修改（入口闸机版本）
- `D:\unitree\r1_showroom_reception_with_gesture.cpp`：未修改（带挥手动作版本）

---

## 用户偏好记录
- 喜欢简洁直接的交流方式
- 代码修改后希望立即看到结果
- 对宇树 R1 机器人开发流程已熟悉

---

## 开机自启动方案讨论
- 用户希望机器人开机后自动运行程序，免去每次手动编译运行
- 提供了 systemd 服务方案：编译后复制到 `/usr/local/bin/`，创建 `r1-show.service` 实现开机自启
- 已通知用户手动编译一次后即可配置自动运行

---

## 使用说明文档
- 创建 `R1展厅控制_使用说明.md`（终版，不再修改）
- 覆盖：程序概述、6段话术、编译、运行、操作方法、LED状态、开机自启动、可配置参数、常见问题
