# R1 底层运动控制指南

本文档用于记录本项目在 R1 底层运动控制开发中的经验、设计准则和测试流程。它不是完整 SDK 教程，而是基于当前项目实测结果整理出来的工程速查文档，方便后续继续开发“手臂移动 + 灵巧手抓取 + 装配流程”。

## 1. 当前阶段结论

目前已经完成并验证：

| 阶段 | 测试程序 | 结果 |
|---|---|---|
| 读取右臂/左臂关节状态 | `R1本体/test_r1_arm_state.cpp` | 已通过，可读取左右臂关节角度 |
| 记录右臂示教点 | `R1本体/test_r1_arm_teach_points.cpp` | 已通过，可记录抓取流程关键点 |
| 右臂姿态保持 | `R1本体/test_r1_right_arm_hold.cpp` | 已通过，右臂可保持姿态，小幅扰动后能恢复 |
| 右肘单关节小幅运动 | `R1本体/test_r1_right_elbow_small.cpp` | 已通过，右肘可小幅弯曲并回位 |
| 右臂多关节移动到示教点 | `R1本体/test_r1_right_arm_move_point.cpp` | 已通过，右臂可到达 `PART_ABOVE` |

当前右臂点位已拆分到独立文件：

```text
R1本体/r1_right_arm_teach_points.hpp
```

后续优化点位时，优先改这个头文件，不要把点位数组散落写在各个测试程序里。

当前重点问题：

| 问题 | 可能原因 | 后续方向 |
|---|---|---|
| 移动过程中有轻微抖动 | `kp/kd`、移动速度、目标点姿态、插值方式还需优化 | 调软参数，增加移动时间，做点位微调工具 |
| 示教点大体可用但不够精细 | 手动摆臂记录角度不适合精修 | 开发键盘微调工具，对每个关节小步调整 |

## 2. 高层运动和底层运动的区别

| 类型 | 我们项目中的例子 | 优点 | 缺点 |
|---|---|---|---|
| 高层运动 | `G1ArmActionClient::ExecuteAction(25)` | 官方封装，安全、省事，适合迎宾动作 | 只能用已有动作，位置不一定适合装配 |
| 底层运动 | 发布 `rt/lowcmd` 控制关节角度 | 可自定义手臂轨迹，适合抓取和装配 | 风险高，需要逐步验证 |

生活类比：

```text
高层运动 = 让机器人执行“预设舞蹈动作”
底层运动 = 你直接控制每个关节转到多少度
```

本项目之前的迎宾动作 ID，例如 `25`、`26`、`33`，属于高层运动。现在开发抓取和装配，需要逐步进入底层运动。

## 3. 底层控制通信链路

底层运动控制主要使用 Unitree SDK2 的 DDS 话题：

```text
程序
  -> Unitree SDK2 ChannelPublisher
  -> rt/lowcmd
  -> R1 底层控制系统
  -> 电机执行
```

同时，程序要订阅机器人状态：

```text
R1 底层状态
  -> rt/lowstate
  -> ChannelSubscriber
  -> 程序读取当前关节角度 q、速度 dq、力矩 tau
```

关键理解：

| 名词 | 含义 |
|---|---|
| `rt/lowstate` | 机器人底层状态话题，用来读取当前关节状态 |
| `rt/lowcmd` | 机器人底层命令话题，用来发送电机目标 |
| `ChannelSubscriber` | DDS 接收者，用来读状态 |
| `ChannelPublisher` | DDS 发送者，用来发命令 |
| `Write()` | 把当前 LowCmd 命令写入 DDS 话题 |
| `q` | 关节角度，单位 rad |
| `dq` | 关节速度 |
| `tau` | 关节力矩 |
| `kp` | 位置刚度，越大越“硬” |
| `kd` | 阻尼，影响收敛和平稳性 |

## 4. R1 右臂关节编号

当前 26DOF 版本中，右臂使用以下 5 个关节：

| IDL | 中文 | 英文代号 | 说明 |
|---|---|---|---|
| 22 | 右肩前后 | `R_SHOULDER_PITCH` | 手臂前后抬起 |
| 23 | 右肩左右 | `R_SHOULDER_ROLL` | 手臂向身体外侧/内侧摆 |
| 24 | 右肩旋转 | `R_SHOULDER_YAW` | 上臂旋转 |
| 25 | 右肘 | `R_ELBOW` | 肘部弯曲 |
| 26 | 右腕旋转 | `R_WRIST_ROLL` | 手腕旋转 |

程序中的右臂数组顺序固定为：

```cpp
[R_SHOULDER_PITCH, R_SHOULDER_ROLL, R_SHOULDER_YAW, R_ELBOW, R_WRIST_ROLL]
```

不要把数组顺序和 IDL 编号混淆。

## 5. 已记录的右臂示教点

当前最新一组示教点保存在：

```text
R1本体/r1_right_arm_teach_points.hpp
```

内容如下：

```cpp
static const RightArmPose kRightArmTeachPoints[] = {
    {"HOME", {0.172741f, -0.120406f, 0.000409f, 0.838659f, -0.010067f}}, // 安全初始位
    {"PART_ABOVE", {-0.746114f, -0.062204f, -0.316353f, 0.729907f, -0.009204f}}, // 零件上方
    {"PART_GRASP", {-0.391189f, -0.060323f, -0.269895f, 0.507046f, -1.600719f}}, // 抓取位置
    {"PART_LIFT", {-0.748439f, -0.104209f, -0.272862f, 0.480116f, -1.600780f}}, // 抓取抬高位
    {"ASSEMBLY_ABOVE", {-0.747348f, 0.191436f, -0.020527f, 0.380311f, -1.600961f}}, // 装配位上方
    {"ASSEMBLY_INSERT", {-0.489747f, 0.126368f, -0.022329f, 0.356968f, -1.600432f}}, // 装配插入位
    {"RETURN_HOME", {0.058477f, -0.103460f, -0.124041f, 1.186665f, 0.035393f}}, // 返回安全位
};
```

这些点位目前是“可用初稿”，不是最终轨迹。后续需要通过微调工具优化。

点位管理原则：

```text
点位数据放在 r1_right_arm_teach_points.hpp
运动流程放在 test_r1_*.cpp
一个文件管“目标在哪里”
一个文件管“怎么移动过去”
```

## 6. 设计准则

### 6.1 一次只验证一个风险点

不要一上来就做完整装配流程。

推荐顺序：

```text
读状态
  -> 保持姿态
  -> 单关节小幅运动
  -> 多关节移动到一个点
  -> 多点轨迹
  -> 加入灵巧手
  -> 加入真实零件
  -> 加入视觉识别
```

这样做的原因是：如果一开始就把手臂、灵巧手、视觉、状态机都接起来，出问题时很难判断是哪一层错了。

### 6.2 程序启动后不要立刻控制机器人

底层控制程序必须先等待用户确认，例如输入 `s` 后再发布 `rt/lowcmd`。

推荐交互：

```text
v = 查看当前角度
t = 查看目标点
s = 开始动作
x = 中断动作
q = 退出
```

这样可以避免程序一启动就让机器人突然动作。

### 6.3 先读当前姿态，再从当前姿态开始运动

不要假设机器人在固定初始位置。

正确做法：

```text
读取当前右臂角度 start_pose
从 start_pose 平滑移动到 target_pose
动作结束后回到 start_pose 或指定安全位
```

这比直接从写死的 `HOME` 开始更安全。

### 6.4 只控制需要控制的关节

当前右臂测试只控制：

```text
22, 23, 24, 25, 26
```

其他关节保持：

```cpp
mode = 0
```

不要在右臂测试里顺手控制头部、腿、腰或左臂。之前头部底层测试出现过整体剧烈抖动，因此头部暂不纳入当前底层实验。

### 6.5 低层命令要持续发布

`rt/lowcmd` 不是“发一次就结束”的命令。底层控制需要周期性发布，当前测试程序使用：

```text
2000 us = 500 Hz
```

理解方式：

```text
不是发一封信让机器人记住
而是像遥控器一样持续给机器人目标位置
```

### 6.6 动作必须插值，不要跳变

不要直接从当前角度跳到目标角度。应该按时间慢慢过渡：

```cpp
current = start + (target - start) * ratio;
```

当前任务 5 使用了平滑比例：

```cpp
smooth = ratio * ratio * (3.0f - 2.0f * ratio);
```

它可以让动作起步和停止更柔和。

### 6.7 参数先保守

当前任务 5 初始参数：

```cpp
kMoveDurationMs = 6000;
kHoldKp = 30.0f;
kHoldKd = 1.5f;
```

如果动作有轻微抖动，优先尝试：

```cpp
kMoveDurationMs = 8000 或 10000;
kHoldKp = 20.0f;
kHoldKd = 2.0f;
```

调参顺序建议：

| 现象 | 优先调整 |
|---|---|
| 动作太快 | 增大 `kMoveDurationMs` |
| 动作太硬、轻微抖动 | 降低 `kHoldKp` |
| 收尾不稳 | 小幅调整 `kHoldKd` |
| 到点姿态不自然 | 优化示教点 |

### 6.8 每次测试都要有中断方式

底层程序必须支持：

```text
x
Ctrl+C
```

中断时要：

```text
停止动作线程
停止 g_control_enabled
发送 disable 帧
退出程序
```

## 7. 抖动排查顺序

如果右臂运动时出现抖动，按以下顺序排查：

| 步骤 | 检查项 | 说明 |
|---|---|---|
| 1 | 是否有多个程序同时发 `rt/lowcmd` | 只能保留一个底层控制程序 |
| 2 | 是否停掉迎宾服务 | 先执行 `sudo systemctl stop r1-show-control.service` |
| 3 | 移动时间是否太短 | 先把 6000ms 改成 8000ms |
| 4 | `kp` 是否偏高 | 先从 30 降到 20 |
| 5 | `kd` 是否不合适 | 试 2.0 或 2.5 |
| 6 | 目标点是否太别扭 | 用微调工具优化示教点 |
| 7 | 是否控制了不该控制的关节 | 确认只启用右臂 22-26 |

常用查看进程：

```bash
ps aux | grep '[r]1'
ps aux | grep '[t]est_r1'
```

必要时停止测试程序：

```bash
pkill -f test_r1_right_arm_move_point
```

## 8. 当前推荐开发路线

### 阶段 A：右臂底层控制稳定化

目标：让右臂移动更稳定、更柔和。

建议任务：

```text
任务 6：右臂示教点微调工具
任务 7：右臂多点轨迹 HOME -> PART_ABOVE -> RETURN_HOME
任务 8：右臂多点轨迹 PART_ABOVE -> PART_GRASP -> PART_LIFT
```

### 阶段 B：加入灵巧手

目标：让手臂到位后，灵巧手完成抓取和释放。

基本流程：

```text
MOVE_TO_PART_ABOVE
  -> MOVE_TO_PART_GRASP
  -> RIGHT_HAND_CUP_GRIP
  -> MOVE_TO_PART_LIFT
  -> MOVE_TO_ASSEMBLY_ABOVE
  -> MOVE_TO_ASSEMBLY_INSERT
  -> RIGHT_HAND_OPEN
  -> RETURN_HOME
```

灵巧手仍然通过：

```text
DDS -> brainco_hand_server -> BrainCo SDK -> Modbus -> 串口 -> 灵巧手
```

底层手臂程序不要直接调用 BrainCo SDK。

### 阶段 C：加入状态机

状态机建议：

```text
RESET
  -> MOVE_TO_PART
  -> APPROACH_PART
  -> GRASP
  -> LIFT_PART
  -> MOVE_TO_ASSEMBLY
  -> ASSEMBLE
  -> RELEASE
  -> RETURN_HOME
```

状态机的作用：

```text
把复杂任务拆成一段一段的小步骤
每个步骤只做一件事
出问题时知道卡在哪一步
```

生活类比：

```text
状态机就像工厂流水线：
每个工位只负责一个动作
必须完成当前工位，才能进入下一个工位
```

### 阶段 D：未来加入视觉识别

现在先不做视觉，但动作列表和状态机要为视觉预留接口。

未来视觉要提供：

```text
零件位置
零件姿态
装配位置
装配方向
是否抓取成功
是否装配成功
```

视觉识别加入后，状态机中的固定点位可以变成：

```text
示教点 + 视觉偏移量
```

## 9. 编译模板

底层测试程序常用编译方式：

```bash
cd ~/unitree_sdk2-1.0

g++ -std=c++17 your_test_file.cpp \
    -I./include -I./thirdparty/include/ddscxx \
    -L./lib/aarch64 -L./thirdparty/lib/aarch64 \
    -lunitree_sdk2 -lddscxx -lddsc -lpthread \
    -Wl,-rpath,./lib/aarch64:./thirdparty/lib/aarch64 \
    -o your_test_file
```

运行：

```bash
./your_test_file eth10
```

测试前建议：

```bash
sudo systemctl stop r1-show-control.service
```

## 10. 新代码注释规则

后续底层运动测试代码必须保持详细注释，尤其是：

```text
每个小功能前加注释
说明这一段代码做什么
说明哪些参数你可以改
说明哪些地方暂时不要改
说明你应该学会什么概念
```

注释中建议明确标注：

```text
你可以改
暂时不要改
你应该学会
安全说明
```

这样代码既能运行，也能作为学习材料。

## 11. 后续更新记录

| 日期 | 内容 |
|---|---|
| 2026-06-01 | 创建本文档，记录任务 1-5 的底层运动控制经验 |
