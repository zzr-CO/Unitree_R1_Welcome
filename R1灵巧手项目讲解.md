# R1 灵巧手项目讲解

## 1. 项目一句话介绍

我基于宇树 R1 机器人和 BrainCo Revo 2 灵巧手，完成了一个面向展厅迎宾场景的机器人交互项目：机器人可以通过遥控器触发语音播报、官方手臂动作和灵巧手手势，并通过 DDS、BrainCo SDK、Modbus 串口链路实现左右手灵巧手控制。

更适合面试时的表达：

> 这是一个 R1 机器人迎宾交互项目。我主要做了遥控器触发的语音与手臂动作联动，并把 BrainCo 灵巧手接入到 Unitree SDK2 的 DDS 通信链路里。项目中我重点解决了灵巧手服务、串口识别、左右手 Modbus ID 不匹配、systemd 后台服务和手势测试工具等工程问题。

## 2. 项目背景与目标

项目目标不是做端到端具身智能算法，而是完成一个可稳定演示的机器人迎宾系统：

- 机器人通电后可作为后台服务运行。
- 使用宇树遥控器触发不同迎宾场景。
- 每个场景包含语音播报和官方预设手臂动作。
- 右手在指定场景中配合做“比耶”等灵巧手动作。
- 左右 BrainCo 灵巧手均可通过独立测试程序验证手势。
- 服务、日志、部署和故障排查流程可复用。

这个项目的价值在于把多个真实机器人系统组件串起来：

- Unitree SDK2
- DDS topic
- R1/G1 官方手臂动作接口
- PCM 音频播放
- BrainCo 灵巧手 SDK
- Modbus / RS485 串口通信
- Linux systemd 后台服务
- Git 版本管理和实验代码隔离

## 3. 系统架构与通信链路

### 3.1 R1 迎宾主程序链路

迎宾主程序负责接收遥控器输入，并触发语音、手臂动作和右手手势。

```text
宇树遥控器
  -> rt/wirelesscontroller
  -> r1_show_control
  -> PCM 语音播放
  -> G1ArmActionClient 官方手臂动作
  -> rt/brainco/right/cmd 右手 DDS 指令
```

主程序不直接控制灵巧手串口，而是只向 DDS topic 发布手势指令。

### 3.2 灵巧手控制链路

灵巧手控制采用“桥接服务”模式：

```text
测试程序 / 迎宾程序
  -> Unitree SDK2 DDS
  -> rt/brainco/left/cmd 或 rt/brainco/right/cmd
  -> brainco_hand_server
  -> BrainCo SDK
  -> Modbus / RS485 串口
  -> BrainCo Revo 2 灵巧手
```

我对这条链路的理解是：

- DDS 负责机器人内部软件模块之间通信。
- `brainco_hand_server` 是翻译官，订阅 DDS topic 并调用 BrainCo SDK。
- Modbus 是服务和灵巧手硬件之间的设备通信协议。
- 串口设备如 `/dev/ttyCH343USB0`、`/dev/ttyCH343USB1` 对应实际硬件连接。
- `slave id` 是 Modbus 设备地址，服务必须用正确 ID 才能控制对应手。

### 3.3 cmd 和 state topic

灵巧手服务里有两类 topic：

```text
rt/brainco/left/cmd
rt/brainco/right/cmd
```

用于发送控制命令。

```text
rt/brainco/left/state
rt/brainco/right/state
```

用于服务反馈手部状态。

如果测试程序提示：

```text
Waiting for connection rt/brainco/left/state
```

通常说明 `brainco_hand_server` 没有运行、左手 worker 没启动、DDS 网口不一致，或服务没有成功绑定左手。

## 4. R1 迎宾主程序功能

当前迎宾主程序通过宇树遥控器触发 4 个场景：

| 按键 | 话术 | 官方动作 |
|---|---|---|
| `← + A` | 话术 1 | action 24 奥特曼光线 |
| `← + B` | 话术 2 | action 25 脸部挥手 + 右手比耶 |
| `← + X` | 话术 3 | action 33 手放胸口鞠躬 |
| `← + Y` | 话术 4 | action 26 高举挥手 |
| `↓ + A` | 四场景循环 | 按顺序循环播放 |
| `SELECT` | 音色切换 | 原始音色 / 东北话音色 |

主程序中的几个重要设计：

- 使用 `rt/wirelesscontroller` 监听遥控器 DDS 消息。
- 使用 `G1ArmActionClient::ExecuteAction()` 调用官方预设动作，避免手写底层电机轨迹。
- 语音使用 PCM 文件，支持双音色切换。
- 日志写入 `/var/log/r1_show_control.log`，便于现场排查。
- 通过 systemd 服务 `r1-show-control.service` 后台运行。
- 右手灵巧手改为按需发布 DDS 指令，避免空闲时持续抢控制权。

## 5. 灵巧手接入与左右手调通

### 5.1 右手接入

右手最先调通，服务识别结果为：

```text
右手串口: /dev/ttyCH343USB1
右手 Modbus ID: 127
DDS topic: rt/brainco/right/cmd
```

右手接入后，我开发了独立测试程序验证手势，不直接改主程序，降低调试风险。确认效果稳定后，再把“右手比耶”合并到迎宾场景。

### 5.2 左手问题定位

左手最初无法控制，排查过程是项目中最有代表性的工程问题。

最开始服务默认认为：

```text
左手 ID = 126
右手 ID = 127
```

但实际扫描发现：

```text
左手串口: /dev/ttyCH343USB0
左手 Modbus ID: 100
左手类型: MediumLeft / 中号左手

右手串口: /dev/ttyCH343USB1
右手 Modbus ID: 127
右手类型: MediumRight / 中号右手
```

也就是说，左手不是 DDS 或测试程序的问题，而是服务中的左手 `slave id` 和实际硬件 ID 不一致。

解决方式：

- 使用 BrainCo SDK 扫描串口和候选 ID。
- 扩展扫描范围，确认左手真实 ID 为 `100`。
- 将旧版 `brainco_hand_server` 中左手 ID 从 `126` 改为 `100`。
- 保留旧版服务的 systemd 自启动机制，减少系统改动。
- 重新编译并启动服务，最终左右手均可正常控制。

这个问题的面试表达：

> 左手不能动时，我没有直接怀疑代码逻辑，而是按通信链路分层排查。先确认服务、DDS topic 和右手链路正常，再用 SDK 扫描 `/dev/ttyCH343USB*` 串口和 Modbus ID，最终发现左手实际 ID 是 100，而服务默认按 126 查找。修改 hand server 的左手 slave id 后，左手正常绑定并可通过 DDS 控制。

## 6. 手势实验程序

我为左右手分别整理了独立手势实验程序：

```text
test_right_gesture_lab.cpp
test_left_gesture_lab.cpp
```

程序特性：

- 通过 Unitree SDK2 DDS 发布 `MotorCmds_` 消息。
- 右手 topic 为 `rt/brainco/right/cmd`。
- 左手 topic 为 `rt/brainco/left/cmd`。
- 主线程读取键盘输入，后台线程每 100ms 持续发布当前手势。
- `Ctrl+C` 退出前发送张开复位姿态。
- 手指控制范围支持 `0.0` 到 `1.0`，已验证半弯曲可用。

手指顺序：

```text
[thumb, thumb_aux, index, middle, ring, pinky]
[拇指, 拇指副指, 食指, 中指, 无名指, 小指]
```

已整理的动作包括：

| 输入 | 中文动作 | 英文标注 | 说明 |
|---|---|---|---|
| `0` | 复位张开 | `open` | 全部手指张开 |
| `1` | 半握拳 | `half-fist` | 所有手指半弯曲 |
| `2` | 剪刀手/比耶 | `v-sign` | 食指和中指伸出 |
| `3` | 大拇指点赞 | `thumb-up` | 大拇指伸出，其余弯曲 |
| `4` | 竖起食指 | `point` | 食指指示 |
| `5` | 电话手势/六 | `shaka` | 拇指和小指伸出 |
| `6` | 轻松自然手 | `relaxed` | 轻微自然弯曲 |
| `8` | OK 手势 | `ok` | 拇指和食指形成 OK 候选姿态 |
| `9` | 三指展示 | `three` | 展示三根手指 |
| `a` | 四指展示 | `four` | 展示四根手指 |
| `c` | 抓取杯子 | `cup-grip` | 半弯曲抓握姿态 |

其中 `w` 和 `b` 是动态演示动作，可用于展示手指连续变化，但暂不作为正式迎宾动作。

## 7. 关键技术难点与解决方案

### 难点 1：理解多层通信链路

问题：

灵巧手不是直接由测试程序控制，容易混淆 DDS、服务、SDK、Modbus、串口之间的关系。

解决：

梳理出稳定链路：

```text
DDS topic -> brainco_hand_server -> BrainCo SDK -> Modbus 串口 -> 灵巧手
```

并明确：

- topic 不是外设，只是软件通信频道。
- `/dev/ttyCH343USB*` 才是串口设备。
- `slave id` 是 Modbus 设备地址。

### 难点 2：左手无法识别

问题：

服务按默认 ID `126` 查找左手，但左手实际 ID 是 `100`。

解决：

通过串口扫描工具确认：

```text
/dev/ttyCH343USB0 -> MediumLeft, ID 100
/dev/ttyCH343USB1 -> MediumRight, ID 127
```

然后修改服务中左手 ID，使服务成功绑定左手。

### 难点 3：systemd 后台服务和手动进程混淆

问题：

手动 `Ctrl+C` 停掉一个前台服务后，右手仍然能动，容易误判服务不需要运行。

原因：

后台还有 `brainco_hand.service` 由 systemd 管理，服务仍在运行或自动重启。

解决：

学会用以下命令区分服务、进程和自启动状态：

```bash
systemctl status brainco_hand.service
systemctl is-enabled brainco_hand.service
ps aux | grep '[b]rainco_hand'
journalctl -u brainco_hand.service -f
```

### 难点 4：多个程序抢控制权导致手抖

问题：

如果主程序和测试程序同时往同一个 DDS topic 发指令，手会在多个目标姿态之间抖动。

解决：

将 R1 主程序改为按需发送右手指令：

- 空闲时不持续发布张开姿态。
- 只有 `←+B` 场景开始时发布比耶。
- 场景结束和程序退出时发布张开复位。

### 难点 5：手臂动作与灵巧手动作协调

问题：

如果手臂动作位置不理想，灵巧手手势本身正确也会影响展示效果。

解决：

优先使用官方动作库，不写底层电机轨迹。通过实机比较动作 `23`、`25`、`31`、`33` 等效果，最终选择更适合展示的动作组合。

## 8. 项目成果与可展示功能

当前项目可展示内容：

- R1 机器人通过遥控器触发 4 个迎宾场景。
- 支持 PCM 语音播报和双音色切换。
- 支持官方手臂动作，如奥特曼光线、脸部挥手、手放胸口、高举挥手。
- 右手可在迎宾场景中配合比耶。
- 左右手均可通过独立测试程序控制。
- 手指支持半弯曲，不只是张开/闭合二值控制。
- 已验证多个可展示手势：比耶、点赞、竖食指、OK、抓杯子、自然手等。
- 通过 systemd 管理迎宾程序和灵巧手服务。
- 建立了日志、进程、服务和串口排查流程。

## 9. 我的主要贡献

面试中可以这样概括个人贡献：

- 基于 Unitree SDK2 编写 R1 迎宾控制程序，实现遥控器触发语音和官方手臂动作。
- 使用 PCM 音频文件实现展厅话术播放，并支持不同音色版本切换。
- 接入 BrainCo 灵巧手服务，理解并梳理 DDS 到 Modbus 串口的控制链路。
- 开发右手和左手手势测试程序，用于独立验证灵巧手动作。
- 通过串口和 Modbus ID 扫描定位左手无法识别问题，确认左手实际 ID 为 `100`。
- 修改服务配置/源码后完成左右手联调。
- 优化主程序中灵巧手指令发布策略，避免测试程序和主程序抢控制权。
- 梳理 systemd 服务管理、日志查看和部署流程，提升项目可维护性。

## 10. 1 分钟面试讲稿

> 我做的是一个基于宇树 R1 的迎宾交互项目。项目里我通过 Unitree SDK2 接收遥控器 DDS 消息，触发 PCM 语音播报和官方预设手臂动作，同时把 BrainCo 灵巧手接入到机器人系统中。灵巧手不是由主程序直接控制串口，而是通过 `brainco_hand_server` 把 DDS topic 上的手势指令转换成 BrainCo SDK 和 Modbus 串口控制。项目中比较关键的问题是左手一开始无法识别，我通过扫描 `/dev/ttyCH343USB*` 串口和 Modbus ID，定位到左手实际 ID 是 100，而服务默认按 126 查找。修改后左右手都可以正常控制。我还写了左右手手势实验程序，验证了比耶、点赞、OK、抓取杯子等动作，并把右手比耶合并到了迎宾场景里。

## 11. 3 分钟面试讲稿

> 这个项目是一个 R1 机器人展厅迎宾系统，目标是让机器人可以通过遥控器触发不同的迎宾场景，每个场景包括语音播报、官方手臂动作，以及部分灵巧手动作。
>
> 主程序基于 Unitree SDK2 开发，订阅 `rt/wirelesscontroller` 这个 DDS topic 来接收遥控器输入。收到按键后，程序会播放对应 PCM 语音文件，并通过 `G1ArmActionClient::ExecuteAction()` 调用宇树官方预设手臂动作。我没有自己写底层电机轨迹，而是优先使用官方动作库，降低安全风险和调试复杂度。程序通过 systemd 部署成后台服务，日志写入 `/var/log/r1_show_control.log`，方便现场维护。
>
> 灵巧手部分采用桥接架构。测试程序或迎宾主程序只往 `rt/brainco/right/cmd` 或 `rt/brainco/left/cmd` 发布 DDS 指令，真正连接硬件的是 `brainco_hand_server`。它订阅 DDS 指令后调用 BrainCo SDK，再通过 Modbus/RS485 串口控制 Revo 2 灵巧手。这个过程中我重点理解了 DDS topic、串口设备和 Modbus slave id 的区别。
>
> 项目中最典型的排查问题是左手不能动。右手能正常控制，但左手服务一直找不到设备。我先确认 DDS 和右手链路没问题，再用 SDK 工具扫描 `/dev/ttyCH343USB0/1/2` 以及不同 Modbus ID，最后发现左手实际在 `/dev/ttyCH343USB0`，ID 是 100，而服务默认用 126 找左手。修改左手 ID 后，服务成功绑定左手，左右手都可以通过 DDS 控制。
>
> 后续我又写了左右手手势实验程序，支持输入编号测试不同手势，包括半握拳、比耶、点赞、竖食指、OK、抓取杯子等。实验中还验证了手指位置可以用 0 到 1 的连续值控制，不只是张开和闭合。最后我把右手比耶合入迎宾主程序，并把主程序改成只在需要手势时短时间发布 DDS 指令，避免和独立测试程序抢控制权导致手抖。

## 12. 面试官可能追问的问题

### Q1：DDS 和 Modbus 在项目里分别做什么？

DDS 是机器人内部软件模块之间的发布订阅通信机制。比如测试程序和 `brainco_hand_server` 之间通过 `rt/brainco/right/cmd` 传递手势指令。

Modbus 是 `brainco_hand_server` 和灵巧手硬件控制板之间的设备通信协议，走串口或 RS485。

简短回答：

> DDS 负责软件模块之间通信，Modbus 负责服务和具体硬件设备之间通信。

### Q2：`rt/brainco/right/cmd` 是外设吗？

不是。它是 DDS topic，也就是软件通信频道。真正的外设是 `/dev/ttyCH343USB*` 这样的串口设备和 BrainCo 灵巧手硬件。

### Q3：`brainco_hand_server` 是什么？

它是一个桥接服务，可以理解为翻译官：

```text
DDS 手势命令 -> BrainCo SDK -> Modbus 串口命令
```

如果它不运行，测试程序发出的 DDS 指令就无法真正到达灵巧手硬件。

### Q4：左手问题最后是怎么解决的？

左手最初不能动，是因为服务默认按 ID `126` 查找左手，但实际左手 ID 是 `100`。我通过串口和全 ID 扫描定位到真实设备信息，然后修改服务里的左手 slave id，最终左手成功绑定并可控。

### Q5：为什么不能多个程序同时控制同一只手？

因为多个程序如果同时往同一个 DDS topic 发不同手势，比如一个发张开、一个发握拳，`brainco_hand_server` 会连续收到互相冲突的目标姿态，手就可能抖动。

### Q6：为什么主程序改成按需发送右手指令？

早期主程序空闲时也持续发布右手张开姿态，这会和独立测试程序抢控制权。改成按需发布后，只有需要右手比耶或退出复位时才短时间发送指令，测试程序和主程序之间更不容易互相干扰。

### Q7：你有没有做运动规划或底层电机控制？

没有夸大成运动规划。手臂部分使用宇树官方预设动作接口 `ExecuteAction()`，灵巧手部分发送归一化手指位置。这个项目重点是机器人应用集成、通信链路调试、服务部署和灵巧手动作验证。

### Q8：这个项目体现了哪些工程能力？

- 能读懂并串联多个 SDK。
- 能用 Linux 和 systemd 管理机器人后台服务。
- 能通过日志、进程、串口和 ID 扫描定位问题。
- 能把实验程序和主程序隔离，先验证再集成。
- 能处理真实硬件调试中常见的不确定性。

## 13. 简历项目描述候选

如果要写进简历，可以压缩成这样：

> **宇树 R1 迎宾交互与 BrainCo 灵巧手联调项目**：基于 Unitree SDK2 开发 R1 展厅迎宾控制程序，实现遥控器触发 PCM 语音播报、官方手臂动作和右手灵巧手比耶；接入 BrainCo Revo 2 灵巧手，通过 DDS topic、`brainco_hand_server`、BrainCo SDK 和 Modbus 串口完成手势控制链路；开发左右手手势实验程序，支持半握拳、比耶、点赞、OK、抓取杯子等动作；通过串口扫描和 Modbus ID 排查定位左手 ID 配置错误，将左手实际 ID `100` 与右手 ID `127` 接入服务，完成左右手控制；使用 systemd 管理迎宾程序和灵巧手服务，完善日志与部署排查流程。

更短版本：

> 基于 Unitree SDK2 与 BrainCo SDK 完成 R1 迎宾交互和灵巧手联调，实现遥控器触发语音、官方手臂动作和左右手手势控制；通过 DDS、Modbus 串口和 systemd 服务完成控制链路搭建，并定位修复左手 Modbus ID 不匹配问题。

## 14. 面试时要避免的说法

不要说：

- “我做了完整具身智能大模型控制。”
- “我做了机器人自主规划。”
- “我直接控制了 R1 手臂底层电机。”
- “DDS topic 是外设。”
- “灵巧手测试程序直接控制串口。”

建议说：

- “我做的是机器人应用层集成和硬件联调。”
- “手臂动作使用宇树官方预设动作接口。”
- “灵巧手通过 DDS 到 hand server，再经 BrainCo SDK 和 Modbus 串口控制。”
- “我重点解决了真实设备中的服务部署、串口识别和 ID 不匹配问题。”

## 15. 后续可以继续增强的方向

如果后续继续推进，可以从这些方向扩展：

- 将左右手手势库合并为统一接口。
- 在迎宾场景中加入更多手势动作。
- 为 `brainco_hand.service` 整理标准部署文档。
- 增加启动时自检：服务是否运行、左右手是否 bound 成功。
- 让主程序支持选择左手、右手或双手手势。
- 记录每个手势的实机照片和最佳参数，形成手势资产库。

## 16. 当前项目关键词

面试准备时需要能解释这些词：

- Unitree SDK2
- DDS topic
- `rt/wirelesscontroller`
- `rt/brainco/right/cmd`
- `rt/brainco/left/cmd`
- `brainco_hand_server`
- BrainCo SDK
- Modbus
- serial port
- slave id
- `/dev/ttyCH343USB0`
- `/dev/ttyCH343USB1`
- systemd
- `systemctl`
- `journalctl`
- PCM audio
- `G1ArmActionClient`
