# R1展厅控制程序 - 技术学习报告

**项目名称：** Unitree R1 展厅接待程序  
**开发时间：** 2026-05-12  
**开发者：** Zirui  
**文档版本：** 1.0  

---

## 📚 目录

1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [核心功能实现](#核心功能实现)
4. [关键技术详解](#关键技术详解)
5. [部署与自动化](#部署与自动化)
6. [问题与解决](#问题与解决)
7. [学习收获](#学习收获)

---

## 📋 项目概述

### **项目目标**
开发一个用于宇树 R1 机器人展厅接待的程序，实现：
- ✅ 语音播报欢迎语
- ✅ 机械臂动作演示
- ✅ 无线遥控器控制
- ✅ 开机自动启动
- ✅ 日志输出与查看

### **硬件平台**
- **机器人：** Unitree R1
- **目标平台：** aarch64 (ARM 64位)
- **网络接口：** eth10
- **开发环境：** Windows + MobaXTerm SSH

---

## 🛠️ 技术栈

### **1. 编程语言**
| 技术 | 版本/标准 | 用途 |
|------|-----------|------|
| **C++** | C++17 | 主程序开发 |
| **Bash** | Shell Script | 部署脚本 |
| **systemd** | - | 服务管理 |

### **2. 核心库与框架**
| 库/框架 | 版本 | 用途 |
|---------|------|------|
| **Unitree SDK2** | 1.0 | 机器人控制（机械臂、LED、DDS通信） |
| **DDS (Data Distribution Service)** | - | 分布式通信中间件 |
| **pthread** | - | 多线程支持 |

### **3. 开发工具**
| 工具 | 用途 |
|------|------|
| **g++** | C++ 编译器（支持 C++17） |
| **MobaXTerm** | SSH 远程连接工具 |
| **systemctl** | systemd 服务管理 |
| **journalctl** | 日志查看工具 |

---

## 🎯 核心功能实现

### **功能1：语音播报**

#### **实现原理**
使用 Unitree SDK2 的 `Audio` 类播放预录制的 MP3 文件。

#### **关键代码**
```cpp
// 初始化音频
unitree::robot::Audio audio;
audio.InitChannel(sdk.get());

// 播放语音
audio.Speak("/path/to/voice.mp3", 50);  // 50% 音量
```

#### **技术要点**
- ✅ 音频文件放在 `voice_pack/` 目录
- ✅ 使用绝对路径或相对于工作目录的路径
- ✅ 音量范围：0-100

---

### **功能2：机械臂动作**

#### **实现原理**
通过 DDS 消息发布机械臂动作指令，R1 机器人接收后执行预设动作。

#### **关键代码**
```cpp
// 机械臂动作 ID（示例）
#define ACT_HIGH_WAVE 26  // 高举手挥手
#define ACT_LOW_WAVE  25  // 低举手挥手

// 发布动作指令
unitree_api::msg::dds_ArmLandingPadArmMickeyArmReq_ arm_cmd;
arm_cmd.arm_action_id = ACT_HIGH_WAVE;
arm_pub->Write(arm_cmd);
```

#### **技术要点**
- ✅ 动作 ID 由机器人固件定义
- ✅ 需要通过 DDS 发布消息
- ✅ 每个场景可以配置不同的动作

---

### **功能3：无线遥控器控制**

#### **实现原理**
解析无线遥控器的按键事件（通过 DDS 接收），根据不同的按键组合触发不同的场景。

#### **关键代码**
```cpp
// 遥控器按键结构体
struct GamepadEvent {
    int buttons;  // 按键位掩码
    float axes[4];  // 摇杆值
};

// 按键检测（← + A）
if ((event.buttons & 0x02) && (event.buttons & 0x10)) {
    // 触发场景0
    audio.Speak(voice_path, 50);
    arm_pub->Write(arm_cmd);
}
```

#### **技术要点**
- ✅ 按键使用位掩码（bitmask）表示
- ✅ ← 键：0x02，A 键：0x10
- ✅ 支持组合键（同时按两个键）

---

### **功能4：开机自启动**

#### **实现原理**
使用 systemd 创建系统服务，配置为开机自启动。

#### **服务文件（/etc/systemd/system/r1-show.service）**
```ini
[Unit]
Description=Unitree R1 Showroom Control
After=network-online.target

[Service]
Type=simple
ExecStart=/usr/local/bin/r1_show_control eth10
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

#### **技术要点**
- ✅ `After=network-online.target`：等网络就绪后启动
- ✅ `Restart=always`：程序崩溃后自动重启
- ✅ `WantedBy=multi-user.target`：多用户模式下启动（开机）

---

### **功能5：日志输出与查看**

#### **实现原理**
通过 systemd 的输出重定向，将程序的 `cout` 输出写入日志文件。

#### **配置方法**
在 `deploy_r1.sh` 中：
```bash
LOG_FILE="/var/log/r1_show_control.log"
ExecStart=/bin/bash -c '${DEPLOY_PATH} ${NETWORK_IF} >> ${LOG_FILE} 2>&1'
```

#### **日志查看命令**
```bash
# 实时查看日志
tail -f /var/log/r1_show_control.log

# 查看 systemd 日志
sudo journalctl -u r1-show.service -f
```

#### **技术要点**
- ✅ `>>`：追加输出到文件
- ✅ `2>&1`：将标准错误也重定向到文件
- ✅ logrotate：自动轮转日志（7天保留）

---

## 🔬 关键技术详解

### **1. DDS (Data Distribution Service)**

#### **什么是 DDS？**
DDS 是一种**分布式通信中间件**，用于实时系统中的数据发布和订阅。

#### **在项目中的作用**
- 接收无线遥控器的按键事件
- 发布机械臂动作指令
- 发布 LED 控制指令

#### **关键概念**
| 概念 | 说明 |
|------|------|
| **Publisher** | 发布者（发送消息） |
| **Subscriber** | 订阅者（接收消息） |
| **Topic** | 主题（消息类型） |
| **QoS** | 服务质量策略 |

#### **代码示例**
```cpp
// 创建发布者
auto arm_pub = sdk.create_publisher<unitree_api::msg::dds_ArmLandingPadArmMickeyArmReq_>("arm_topic");

// 发布消息
unitree_api::msg::dds_ArmLandingPadArmMickeyArmReq_ msg;
msg.arm_action_id = 26;
arm_pub->Write(msg);
```

---

### **2. Unitree SDK2 框架**

#### **SDK 结构**
```
unitree_sdk2-1.0/
├── include/              # 头文件
│   ├── unitree_api/
│   │   └── msg/        # DDS 消息定义
│   └── unitree/
│       └── robot/      # 机器人控制类
│           ├── Audio.h
│           ├── Led.h
│           └── Channel.h
├── lib/aarch64/         # 编译好的库（aarch64）
└── thirdparty/          # 第三方库（DDS）
    └── lib/aarch64/
```

#### **核心类**
| 类 | 功能 |
|----|------|
| `ChannelFactory` | DDS 通信通道管理 |
| `Audio` | 音频播放 |
| `Led` | LED 控制 |
| `RobotState` | 机器人状态查询 |

---

### **3. C++17 特性使用**

#### **使用的特性**
```cpp
// 1. 结构化绑定（C++17）
auto [it, inserted] = my_map.insert({key, value});

// 2. if 初始化语句（C++17）
if (auto it = my_map.find(key); it != my_map.end()) {
    // 使用 it
}

// 3. 折叠表达式（C++17）
template<typename... Args>
auto sum(Args... args) {
    return (args + ...);
}

// 4. 文件系统库（C++17）
#include <filesystem>
namespace fs = std::filesystem;
fs::path p = "/path/to/file";
```

#### **编译选项**
```bash
g++ -std=c++17 r1_show_control.cpp -o r1_show_control
```

---

### **4. systemd 服务管理**

#### **服务生命周期**
```
1. 系统启动
   ↓
2. network-online.target 就绪
   ↓
3. r1-show.service 启动
   ↓
4. 执行 ExecStart 命令
   ↓
5. 程序运行
   ↓
6. 如果崩溃，等待 RestartSec 后重启
```

#### **常用命令**
```bash
# 启动服务
sudo systemctl start r1-show.service

# 停止服务
sudo systemctl stop r1-show.service

# 重启服务
sudo systemctl restart r1-show.service

# 查看状态
sudo systemctl status r1-show.service

# 启用开机自启动
sudo systemctl enable r1-show.service
```

---

## 🚀 部署与自动化

### **deploy_r1.sh 脚本功能**

#### **自动化流程**
```
1. 检查源文件
   ↓
2. 创建日志文件
   ↓
3. 编译 C++ 代码
   ↓
4. 部署到 /usr/local/bin/
   ↓
5. 创建/更新 systemd 服务
   ↓
6. 配置 logrotate
   ↓
7. 重启服务
   ↓
8. 显示状态和日志
```

#### **脚本核心代码**
```bash
# 编译
g++ -std=c++17 r1_show_control.cpp \
    -I./include -I./thirdparty/include/ddscxx \
    -L./lib/aarch64 -L./thirdparty/lib/aarch64 \
    -lunitree_sdk2 -lddscxx -lddsc -lpthread \
    -Wl,-rpath,./lib/aarch64:./thirdparty/lib/aarch64 \
    -o r1_show_control

# 部署
sudo cp r1_show_control /usr/local/bin/
sudo chmod +x /usr/local/bin/r1_show_control

# 创建 systemd 服务
sudo tee /etc/systemd/system/r1-show.service > /dev/null <<EOF
[Service]
ExecStart=/bin/bash -c '/usr/local/bin/r1_show_control eth10 >> /var/log/r1_show_control.log 2>&1'
Restart=always
EOF

# 启动服务
sudo systemctl daemon-reload
sudo systemctl enable r1-show.service
sudo systemctl restart r1-show.service
```

---

### **logrotate 日志轮转**

#### **配置文件（/etc/logrotate.d/r1-show）**
```
/var/log/r1_show_control.log {
    daily           # 每天轮转
    rotate 7        # 保留7天
    compress        # 压缩旧日志
    delaycompress   # 延迟压缩（下次轮转时压缩）
    missingok       # 日志不存在也不报错
    notifempty      # 空文件不轮转
    create 666 root root  # 新建日志文件权限
}
```

#### **工作原理解释**
```
第1天：r1_show_control.log (当前日志)
第2天：r1_show_control.log.1 (昨天的日志)
第3天：r1_show_control.log.2.gz (前天的日志，已压缩)
...
第8天：删除最早的第7个日志
```

---

## 🐛 问题与解决

### **问题1：程序不执行命令**

#### **可能原因**
1. 程序崩溃了
2. 无线遥控器休眠了
3. DDS 通信失败

#### **排查方法**
```bash
# 1. 查看服务状态
sudo systemctl status r1-show.service

# 2. 查看实时日志
tail -f /var/log/r1_show_control.log

# 3. 查看 DDS 通信日志
sudo journalctl -u r1-show.service | grep -i dds
```

#### **解决方法**
- ✅ 如果是程序崩溃：`Restart=always` 会自动重启
- ✅ 如果是遥控器休眠：按任意键唤醒
- ✅ 如果是 DDS 失败：检查网络接口（eth10）

---

### **问题2：编译失败**

#### **常见错误**
```
fatal error: unitree_api/msg/dds_xxx.h: No such file or directory
```

#### **解决方法**
```bash
# 检查 include 路径
ls ./include/unitree_api/msg/

# 确认编译命令包含 -I./include
g++ -std=c++17 r1_show_control.cpp -I./include ...
```

---

### **问题3：日志文件不存在**

#### **原因**
- 服务没启动
- 日志路径配置错误

#### **解决方法**
```bash
# 1. 手动创建日志文件
sudo touch /var/log/r1_show_control.log
sudo chmod 666 /var/log/r1_show_control.log

# 2. 启动服务
sudo systemctl start r1-show.service

# 3. 查看日志
tail -f /var/log/r1_show_control.log
```

---

## 📖 学习收获

### **1. Linux 系统编程**
- ✅ 掌握了 systemd 服务管理
- ✅ 理解了日志轮转机制（logrotate）
- ✅ 学会了 Bash 脚本编写

### **2. C++ 高级特性**
- ✅ 使用了 C++17 标准
- ✅ 理解了多线程编程（pthread）
- ✅ 掌握了 DDS 通信编程

### **3. 机器人控制**
- ✅ 理解了 Unitree SDK2 框架
- ✅ 掌握了机械臂控制方法
- ✅ 学会了 DDS 消息发布/订阅

### **4. 部署与自动化**
- ✅ 实现了自动化编译、部署、启动
- ✅ 配置了开机自启动
- ✅ 实现了日志管理和查看

---

## 🎯 下一步学习计划

### **短期目标（1-2周）**
- [ ] 深入学习 DDS 通信机制
- [ ] 掌握 Unitree SDK2 的更多 API
- [ ] 优化机械臂动作流畅度

### **中期目标（1个月）**
- [ ] 添加手势识别功能（参考 `r1_showroom_reception_with_gesture.cpp`）
- [ ] 实现场景切换动画
- [ ] 完善错误处理和日志

### **长期目标（项目结束）**
- [ ] 掌握完整的机器人控制系统开发
- [ ] 能够独立开发类似的展厅接待程序
- [ ] 形成可复用的代码库和文档

---

## 📂 附录：文件清单

### **源代码**
| 文件 | 说明 |
|------|------|
| `r1_show_control.cpp` | 主程序（语音+机械臂） |
| `r1_showroom_reception.cpp` | 展厅接待程序（基础版） |
| `r1_showroom_reception_with_gesture.cpp` | 展厅接待程序（带手势识别） |

### **部署脚本**
| 文件 | 说明 |
|------|------|
| `deploy_r1.sh` | 一键部署脚本 |
| `deploy_r1使用说明.md` | 部署脚本使用说明 |

### **文档**
| 文件 | 说明 |
|------|------|
| `R1展厅控制_使用说明.md` | 程序使用说明 |
| `systemd使用指南.md` | systemd 技术文档 |
| `R1展厅控制程序_技术学习报告.md` | 本文档 |

---

## ✅ 总结

通过今天的学习和开发，我们实现了：
1. ✅ **完整的展厅接待功能**（语音+机械臂+遥控器）
2. ✅ **自动化部署流程**（一键编译、部署、启动）
3. ✅ **开机自启动**（systemd 服务）
4. ✅ **日志管理**（输出重定向 + logrotate）
5. ✅ **技术文档**（使用说明 + 技术学习报告）

**关键技能掌握：**
- Unitree SDK2 框架
- DDS 通信编程
- systemd 服务管理
- Bash 脚本编写
- C++17 特性

---

**文档完成日期：** 2026-05-12  
**下一步：** 继续完善功能，添加手势识别，优化用户体验
